# virtual environment + caches
.venv/
__pycache__/
*.pyc
*.pyo

# downloaded model (fetched at first run by ensure_model())
models/
*.tmp

# recordings + previews
recordings/

# local Unity Capture driver download (re-fetched by setup_virtualcam.bat)
tools/
